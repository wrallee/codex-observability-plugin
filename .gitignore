node_modules/
*.log
.DS_Store

# Codex writes a per-rollout dedup sidecar next to each transcript at runtime.
*.jsonl.langfuse

# Local debugging
debug.log

# Keep the standalone hook in Git for marketplace installs.
plugins/tracing/dist/*
!plugins/tracing/dist/index.mjs
